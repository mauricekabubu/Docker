version: 29.5.3

services:
  flask:
    build: .
    container_name: flask_app
    ports:
      - "5000:5000"
    volumes:
      - .:/main
    environment:
      - FLASK_ENV=development
      - FLASK_APP=view.py
    command: flask run --host=0.0.0.0